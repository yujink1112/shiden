#共通設定

- 回答は日本語で行う。
- ビルドは不要。
- 既存の設計・命名・データ構造に合わせて変更する。
- public/data/stages.json を編集するときは JSON の構造を壊さない。
- ユーザー未依頼の大きなリファクタは行わない。
- ファイルはUTF-8指定で読み込むこと。
- 作業の過程でスクリプトが作られるなら、ローカルで使えるようにtoolフォルダに書き出して説明書を追加して欲しい。
- 画面部品の追加時、スマホのタップハイライトを消すこと。